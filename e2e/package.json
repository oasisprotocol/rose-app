{
  "name": "@oasisprotocol/rose-app-e2e",
  "private": true,
  "license": "See License in ../LICENSE",
  "type": "module",
  "scripts": {
    "install:deps": "pnpm exec playwright install --with-deps chromium",
    "test": "pnpm exec playwright test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^22.7.5"
  }
}
